apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: web-bots-reader
  namespace: bots
subjects:
  - kind: ServiceAccount
    name: web
    namespace: platform
roleRef:
  kind: Role
  name: web-bots-reader
  apiGroup: rbac.authorization.k8s.io
