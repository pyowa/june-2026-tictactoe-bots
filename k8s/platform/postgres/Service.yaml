apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: platform
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
      nodePort: 30003
