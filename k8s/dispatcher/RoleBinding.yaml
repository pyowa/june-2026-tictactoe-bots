apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: dispatcher
  namespace: bots
subjects:
  - kind: ServiceAccount
    name: dispatcher
    namespace: bots
roleRef:
  kind: Role
  name: dispatcher
  apiGroup: rbac.authorization.k8s.io
